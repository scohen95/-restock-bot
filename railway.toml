[build]
builder = "dockerfile"

[deploy]
restartPolicyType = "always"
restartPolicyMaxRetries = 10
numReplicas = 1

# No port needed — this is a worker, not a web server.
# Railway will keep it running 24/7.
